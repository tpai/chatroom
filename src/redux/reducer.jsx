import channelList from 'redux/modules/channelList';
import currentChannel from 'redux/modules/currentChannel';
import messageList from 'redux/modules/messageList';
import userData from 'redux/modules/userData';
import userList from 'redux/modules/userList';

export default {
    channelList,
    currentChannel,
    messageList,
    userData,
    userList
};
