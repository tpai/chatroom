import { reducer as profile } from 'redux/modules/profile';
import { reducer as users } from 'redux/modules/users';
import { reducer as messages } from 'redux/modules/messages';

export default {
  profile,
  users,
  messages,
};
